import Navbar from '@/components/Navbar';
import Footer from '@/components/Footer';
import ContactModal from '@/components/ContactModal';
import ContactLargeFinal from '@/components/ContactLargeFinal';

export default function AboutPage() {
  return (
    <>
      <Navbar />

      {/* Hero */}
      <section className="max-w-6xl mx-auto px-6 pt-20 pb-10">
        <div className="grid md:grid-cols-2 gap-10 items-center">
          <div>
            <h1 className="text-4xl md:text-5xl font-extrabold leading-tight">
              About LayerZero
            </h1>
            <p className="mt-4 text-lg text-gray-600">
              We’re on a mission to make AI safe for every organization—
              enforcing data policies in real time so teams can innovate with confidence.
            </p>
            <div className="mt-6 flex gap-3">
              <a href="/trust" className="px-5 py-3 rounded-2xl border">
                Security &amp; Trust
              </a>
              <a href="/admin" className="px-5 py-3 rounded-2xl bg-black text-white">
                Launch Live Demo
              </a>
            </div>
          </div>

          {/* Mirror visual language of the dashboard preview card */}
          <div className="rounded-2xl bg-white shadow p-6">
            <div className="text-sm text-gray-500 mb-2">What we do</div>
            <div className="rounded-xl border p-4">
              <div className="grid sm:grid-cols-2 gap-3">
                <div className="rounded-lg border p-3">
                  <div className="text-xs text-gray-500">PII / GDPR</div>
                  <div className="text-sm font-medium mt-1">
                    Detect &amp; redact names, emails, phones, addresses.
                  </div>
                </div>
                <div className="rounded-lg border p-3">
                  <div className="text-xs text-gray-500">Source Code &amp; Secrets</div>
                  <div className="text-sm font-medium mt-1">
                    Catch tokens, keys, repo refs, code fragments.
                  </div>
                </div>
                <div className="rounded-lg border p-3">
                  <div className="text-xs text-gray-500">Human-in-the-Loop</div>
                  <div className="text-sm font-medium mt-1">
                    Escalate risky prompts for approval—then release safely.
                  </div>
                </div>
                <div className="rounded-lg border p-3">
                  <div className="text-xs text-gray-500">Audit &amp; Export</div>
                  <div className="text-sm font-medium mt-1">
                    Export decisions to your SIEM (CSV/JSON/API).
                  </div>
                </div>
              </div>
              <div className="mt-4 text-right">
                <a className="text-sm underline" href="/admin">See full dashboard →</a>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Mission & Values */}
      <section className="max-w-6xl mx-auto px-6 pb-12">
        <div className="grid md:grid-cols-3 gap-6">
          <div className="md:col-span-2">
            <h2 className="text-2xl font-bold">Our Mission</h2>
            <p className="mt-3 text-gray-600">
              Empower every team to adopt AI safely. We combine real-time policy enforcement
              with transparent controls so security and compliance leaders can move fast—
              without compromising on data protection.
            </p>
          </div>
          <div className="rounded-2xl border bg-white p-5">
            <div className="text-sm uppercase tracking-wider text-gray-500 mb-2">Core Values</div>
            <ul className="space-y-2 text-sm text-gray-700">
              <li>• Security as a Product</li>
              <li>• Transparent by Default</li>
              <li>• Human-Centered Controls</li>
              <li>• Practical, not Theoretical</li>
            </ul>
          </div>
        </div>
      </section>

      {/* Milestones */}
      <section className="max-w-6xl mx-auto px-6 pb-14">
        <div className="rounded-2xl border bg-white p-6">
          <div className="text-sm uppercase tracking-wider text-gray-500 mb-4">
            Milestones
          </div>
          <div className="grid md:grid-cols-4 gap-4">
            {[
              { k: '2019', v: 'First PII redaction engine' },
              { k: '2021', v: 'Human-in-the-loop approvals' },
              { k: '2023', v: 'Multi-region routing + zero retention' },
              { k: '2024', v: 'SIEM exports and SAML/SCIM' },
            ].map((m) => (
              <div key={m.k} className="rounded-xl border p-4">
                <div className="text-xs text-gray-500">{m.k}</div>
                <div className="mt-1 font-medium">{m.v}</div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Black CTA Banner */}
      <section className="max-w-6xl mx-auto px-6 pb-14">
        <div className="rounded-2xl bg-black text-white p-8 md:p-10 flex flex-col md:flex-row md:items-center md:justify-between gap-4">
          <div>
            <div className="text-xl font-semibold">The foundation of safe AI</div>
            <div className="text-sm text-gray-300">
              Zero-retention mode. Region routing. Audit exports.
            </div>
          </div>
          <div className="flex gap-3">
            <a className="px-4 py-2 rounded-2xl bg-white text-black" href="/trust">
              Security &amp; Trust
            </a>
            <a className="px-4 py-2 rounded-2xl border border-white" href="/admin">
              Launch Demo
            </a>
          </div>
        </div>
      </section>

      {/* Contact block (same component you use site-wide) */}
      <div className="max-w-6xl mx-auto px-6 pb-16">
        <ContactLargeFinal />
      </div>

      <Footer />
      <ContactModal />
    </>
  );
}