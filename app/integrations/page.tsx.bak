import Navbar from '@/components/Navbar';
import Footer from '@/components/Footer';
import BlackCTABanner from '@/components/BlackCTABanner';
import ContactLargeFinal from '@/components/ContactLargeFinal';
import ContactModal from '@/components/ContactModal';

export default function IntegrationsPage() {
  return (
    <>
      <Navbar />

      {/* Hero */}
      <section className="max-w-6xl mx-auto px-6 pt-20 pb-10">
        <h1 className="text-4xl md:text-5xl font-extrabold leading-tight">
          Works where you do
        </h1>
        <p className="mt-4 text-lg text-gray-600 max-w-3xl">
          Drop-in with OpenAI and Azure OpenAI—no app rewrites. Export signals to your SIEM.
          Connect identity and provisioning with SAML SSO and SCIM.
        </p>
      </section>

      {/* Cards: model providers */}
      <section className="max-w-6xl mx-auto px-6 pb-12">
        <div className="text-xs uppercase tracking-wider text-gray-500 mb-4">Model Providers</div>
        <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">
          {[
            { name: 'OpenAI', desc: 'Native support, policy enforcement at prompt/response.' },
            { name: 'Azure OpenAI', desc: 'Enterprise-grade routing + Azure controls.' },
            { name: 'Custom APIs', desc: 'Attach to proprietary or hosted models via proxy.' },
          ].map((x) => (
            <div key={x.name} className="rounded-xl border bg-white p-5">
              <div className="font-semibold">{x.name}</div>
              <div className="text-sm text-gray-600 mt-2">{x.desc}</div>
            </div>
          ))}
        </div>
      </section>

      {/* Enterprise integrations */}
      <section className="max-w-6xl mx-auto px-6 pb-12">
        <div className="text-xs uppercase tracking-wider text-gray-500 mb-4">Enterprise</div>
        <div className="grid sm:grid-cols-3 gap-4">
          {[
            { name: 'SAML SSO', desc: 'Centralized auth with your IdP.' },
            { name: 'SCIM', desc: 'Automated user lifecycle management.' },
            { name: 'SIEM Export', desc: 'CSV/JSON/API for Splunk, Datadog, more.' },
          ].map((x) => (
            <div key={x.name} className="rounded-xl border bg-white p-5">
              <div className="font-semibold">{x.name}</div>
              <div className="text-sm text-gray-600 mt-2">{x.desc}</div>
            </div>
          ))}
        </div>
      </section>

      {/* How it connects */}
      <section className="max-w-6xl mx-auto px-6 pb-14">
        <div className="rounded-2xl border bg-white p-6">
          <div className="text-xs uppercase tracking-wider text-gray-500">How it connects</div>
          <ul className="mt-3 space-y-3 text-sm">
            <li className="flex items-start gap-3">
              <span className="mt-1 inline-block h-2 w-2 rounded-full bg-black"></span>
              <div><span className="font-medium">Proxy mode:</span> route API calls through LayerZero for inspection & redaction.</div>
            </li>
            <li className="flex items-start gap-3">
              <span className="mt-1 inline-block h-2 w-2 rounded-full bg-black"></span>
              <div><span className="font-medium">SDKs:</span> light-touch clients for Node and Python.</div>
            </li>
            <li className="flex items-start gap-3">
              <span className="mt-1 inline-block h-2 w-2 rounded-full bg-black"></span>
              <div><span className="font-medium">No-code:</span> configure policies in the admin dashboard.</div>
            </li>
          </ul>
        </div>
      </section>

      <BlackCTABanner />
      <ContactLargeFinal />

      <Footer />
      <ContactModal />
    </>
  );
}