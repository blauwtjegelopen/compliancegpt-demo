import Navbar from '@/components/Navbar';
import Footer from '@/components/Footer';
import BlackCTABanner from '@/components/BlackCTABanner';
import ContactLargeFinal from '@/components/ContactLargeFinal';
import ContactModal from '@/components/ContactModal';

export default function TrustPage() {
  return (
    <>
      <Navbar />

      {/* Hero Section */}
      <section className="max-w-6xl mx-auto px-6 pt-20 pb-16">
        <h1 className="text-4xl md:text-5xl font-extrabold leading-tight">
          Security &amp; Trust
        </h1>
        <p className="mt-4 text-lg text-gray-600 max-w-3xl">
          LayerZero is built with compliance and security at its core.
          From GDPR to SOC 2, we help you enforce policies, redact sensitive
          data, and ensure AI adoption meets enterprise standards.
        </p>
      </section>

      {/* Certifications / Controls */}
      <section className="max-w-6xl mx-auto px-6 pb-12">
        <div className="text-xs uppercase tracking-wider text-gray-500 mb-6">
          Certified &amp; Compliant
        </div>
        <div className="flex flex-wrap items-center gap-x-8 gap-y-4 opacity-80 text-gray-700 font-medium">
          <div>GDPR</div>
          <div>SOC 2</div>
          <div>ISO 27001</div>
          <div>HIPAA</div>
          <div>PCI DSS</div>
        </div>
      </section>

      {/* Feature Blocks */}
      <section className="max-w-6xl mx-auto px-6 pb-14 grid sm:grid-cols-3 gap-6">
        <div className="rounded-xl border bg-white p-6">
          <div className="font-semibold">PII &amp; Sensitive Data Redaction</div>
          <div className="text-sm text-gray-600 mt-2">
            Detect and redact emails, phone numbers, addresses, names,
            financial data, and more before they leave your tenant.
          </div>
        </div>
        <div className="rounded-xl border bg-white p-6">
          <div className="font-semibold">Source Code &amp; Secrets Guard</div>
          <div className="text-sm text-gray-600 mt-2">
            Block code fragments, API keys, repository links, and confidential
            developer content inline.
          </div>
        </div>
        <div className="rounded-xl border bg-white p-6">
          <div className="font-semibold">Human-in-the-Loop Escalation</div>
          <div className="text-sm text-gray-600 mt-2">
            Send risky prompts to approvers for review and release,
            ensuring safe collaboration without slowing teams down.
          </div>
        </div>
      </section>

      {/* CTA Banner */}
      <BlackCTABanner />

      {/* Contact */}
      <ContactLargeFinal />

      <Footer />
      <ContactModal />
    </>
  );
}